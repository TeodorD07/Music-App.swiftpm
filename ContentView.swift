import SwiftUI

struct ContentView: View {
    
    var body: some View {
        VStack {
            Text("hello")
                .foregroundColor(.white)
                .font(.largeTitle)
            
        }
        .background(.blue)
    }
}
