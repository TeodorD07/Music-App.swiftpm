import SwiftUI

struct SongLibrary: View {
    @State var artistArray : [Library] = []
    var body: some View {
        VStack {
            
            List(){
                Button("artist") {
                    
                }
            }
            
        }
    }
}
